<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | DigitalMente</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        body { padding-top: 70px; background: #f8f9fa; }
        .navbar-brand { font-weight: bold; letter-spacing: 1px; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-lightbulb-fill"></i> DigitalMente
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mr-auto">
                    <% 
                    // Move a checagem para uma variável local do EJS, simplificando o IF
                    const isProf = user && (user.tipo === 'professor' || user.tipo === 'gestao');
                    const isAluno = user && user.tipo === 'aluno';
                    const isAdmin = user && user.tipo === 'gestao';
                    %>

                    <% if (isProf) { %>
                    <li class="nav-item"><a class="nav-link" href="/home">Início Prof.</a></li>
                    <li class="nav-item"><a class="nav-link" href="/cursos">Gerenciar Cursos</a></li>
                    <% } %>
                    <% if (isAluno) { %>
                        <li class="nav-item"><a class="nav-link" href="/aluno/dashboard">Meus Cursos</a></li>
                    <% } %>
                    <% if (isAdmin) { %>
                        <li class="nav-item"><a class="nav-link text-warning" href="/admin/dashboard">Gestão Total</a></li>
                    <% } %>
                </ul>
                
                <ul class="navbar-nav ml-auto">
                    <% if (user) { %>
                    <li class="nav-item">
                        <span class="navbar-text text-white mr-3">
                            Olá, <strong><%= user.nome %></strong> (<%= user.tipo %>)
                        </span>
                    </li>
                    <li class="nav-item">
                        <a href="/logout" class="btn btn-outline-light btn-sm mt-1">Sair</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item"><a href="/login" class="btn btn-outline-light btn-sm mt-1">Entrar</a></li>
                    <li class="nav-item ml-2"><a href="/cadastro" class="btn btn-outline-light btn-sm mt-1">Professor</a></li>
                    <li class="nav-item ml-2"><a href="/aluno/cadastro" class="btn btn-outline-light btn-sm mt-1">Aluno</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <% 
        // Manter o fallback seguro na View para o flash, já que ele é o ponto mais frágil.
        %>
        <% if ((success || []).length > 0) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill"></i> <%= success.join('<br>') %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
        <% } %>

        <% if ((error || []).length > 0) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i> <%= error.join('<br>') %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
        <% } %>
        
        <%- body %>
    </main>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>